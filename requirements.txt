# Core (required) — everything below is either stdlib-adjacent or pure-Python,
# so the app runs fully offline with just these two installs.
flask>=3.0
requests>=2.31

# Optional — only needed if you want to expose the 4 tools over the REAL
# MCP protocol (stdio transport) for use from Claude Desktop / other MCP
# clients via mcp_stdio_server.py. Not required to run the web dashboard.
# mcp>=1.2
